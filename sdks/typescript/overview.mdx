---
title: "TypeScript SDK"
description: "Give your AI agents email capabilities with the official TypeScript SDK"
icon: "/icons-svg/docs.svg"
---

## Installation

Install the SDK using your preferred package manager:

<Tabs>
  <Tab title="npm">
    ```bash
    npm install jasni-sdk
    ```
  </Tab>
  <Tab title="yarn">
    ```bash
    yarn add jasni-sdk
    ```
  </Tab>
  <Tab title="pnpm">
    ```bash
    pnpm add jasni-sdk
    ```
  </Tab>
</Tabs>

## Quick Start

```typescript
import { Jasni } from 'jasni-sdk'

// Initialize the client with your API key
const jasni = new Jasni('jsk_your_api_key')

// List your agent's inboxes
const { accounts } = await jasni.accounts.list()
console.log(accounts)

// Let your agent send an email
const result = await jasni.emails.send({
  from: 'agent@yourdomain.com',
  to: 'user@example.com',
  subject: 'Your request has been processed',
  text: 'I have completed the task you requested. Here are the results...',
})
console.log('Agent sent email:', result.messageId)
```

## Configuration

The SDK accepts optional configuration parameters:

```typescript
const jasni = new Jasni('jsk_your_api_key', {
  baseUrl: 'https://api.jasni.ai', // Optional: custom base URL
})
```

### Configuration Options

| Option | Type | Default | Description |
|--------|------|---------|-------------|
| `baseUrl` | `string` | `https://api.jasni.ai` | API base URL |

## Available Resources

The Jasni client provides access to the following resources:

<CardGroup cols={2}>
  <Card title="Accounts" icon="/icons-svg/inbox.svg" href="/sdks/typescript/accounts">
    Manage your agent's inboxes
  </Card>
  <Card title="Emails" icon="/icons-svg/email.svg" href="/sdks/typescript/emails">
    Send, receive, and process emails
  </Card>
  <Card title="Drafts" icon="/icons-svg/compose.svg" href="/sdks/typescript/drafts">
    Create and manage email drafts
  </Card>
  <Card title="Labels" icon="/icons-svg/label.svg" href="/sdks/typescript/labels">
    Organize emails with labels
  </Card>
  <Card title="Webhooks" icon="/icons-svg/webhook.svg" href="/sdks/typescript/webhooks">
    Enable real-time reactions
  </Card>
</CardGroup>

## TypeScript Support

The SDK is written in TypeScript and includes full type definitions. Import types as needed:

```typescript
import type { 
  Email, 
  Account, 
  Label, 
  Webhook,
  SendEmailOptions,
  ListEmailsOptions,
  Draft,
  EmailPreview,
} from 'jasni-sdk'
```

## Requirements

- Node.js 18 or higher
- TypeScript 4.7+ (for TypeScript users)

## Next Steps

<CardGroup cols={2}>
  <Card title="Accounts" icon="/icons-svg/inbox.svg" href="/sdks/typescript/accounts">
    Learn how to manage accounts
  </Card>
  <Card title="Error Handling" icon="/icons-svg/alert.svg" href="/sdks/typescript/errors">
    Handle errors gracefully
  </Card>
</CardGroup>
