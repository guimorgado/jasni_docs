---
title: "Quickstart"
description: "Give your AI agent an email inbox in under 5 minutes"
icon: "/icons-svg/rocket.svg"
---

## Before You Start

You'll need:

1. A Jasni account - [Sign up free](https://app.jasni.ai)
2. An API key - Get one from **Developers** → **API Keys** in your [dashboard](https://app.jasni.ai)
3. An inbox for your agent (create one in **My Emails** or via the API)

## Send Your Agent's First Email

<Tabs>
  <Tab title="TypeScript SDK">
    ```bash
    npm install jasni-sdk
    ```
    
    ```typescript
    import { Jasni } from 'jasni-sdk'

    const jasni = new Jasni('jsk_your_api_key')

    const result = await jasni.emails.send({
      from: 'you@yourdomain.com',
      to: 'recipient@example.com',
      subject: 'Hello from Jasni!',
      text: 'This is my first email sent via Jasni API.',
      html: '<h1>Hello!</h1><p>This is my first email sent via Jasni API.</p>'
    })

    console.log('Sent:', result.messageId)
    ```
  </Tab>
  <Tab title="cURL">
    ```bash
    curl -X POST https://api.jasni.ai/api/v1/emails/send \
      -H "Authorization: Bearer jsk_your_api_key" \
      -H "Content-Type: application/json" \
      -d '{
        "from": "you@yourdomain.com",
        "to": "recipient@example.com",
        "subject": "Hello from Jasni!",
        "text": "This is my first email sent via Jasni API.",
        "html": "<h1>Hello!</h1><p>This is my first email sent via Jasni API.</p>"
      }'
    ```
  </Tab>
  <Tab title="JavaScript">
    ```javascript
    const response = await fetch('https://api.jasni.ai/api/v1/emails/send', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer jsk_your_api_key',
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        from: 'you@yourdomain.com',
        to: 'recipient@example.com',
        subject: 'Hello from Jasni!',
        text: 'This is my first email sent via Jasni API.',
        html: '<h1>Hello!</h1><p>This is my first email sent via Jasni API.</p>'
      }),
    });

    const data = await response.json();
    console.log(data);
    ```
  </Tab>
  <Tab title="Python SDK">
    ```bash
    pip install jasni-sdk
    ```
    
    ```python
    from jasni import Jasni

    jasni = Jasni('jsk_your_api_key')

    result = jasni.emails.send(
        from_='you@yourdomain.com',
        to='recipient@example.com',
        subject='Hello from Jasni!',
        text='This is my first email sent via Jasni API.',
        html='<h1>Hello!</h1><p>This is my first email sent via Jasni API.</p>'
    )

    print('Sent:', result.message_id)
    ```
  </Tab>
</Tabs>

### Response

```json
{
  "success": true,
  "data": {
    "messageId": "<abc123@yourdomain.com>",
    "from": "you@yourdomain.com",
    "to": ["recipient@example.com"],
    "subject": "Hello from Jasni!",
    "savedToSent": true
  }
}
```

<Check>
  Your agent just sent its first email autonomously.
</Check>

## Read Your Agent's Inbox

Let your agent check its inbox for incoming messages:

<Tabs>
  <Tab title="TypeScript SDK">
    ```typescript
    const { emails } = await jasni.emails.list({
      account: 'you@yourdomain.com',
      folder: 'INBOX',
      limit: 10,
    })

    emails.forEach(email => {
      console.log(`From: ${email.from} - Subject: ${email.subject}`)
    })
    ```
  </Tab>
  <Tab title="cURL">
    ```bash
    curl -X GET "https://api.jasni.ai/api/v1/emails?account=you@yourdomain.com&folder=INBOX&limit=10" \
      -H "Authorization: Bearer jsk_your_api_key"
    ```
  </Tab>
  <Tab title="JavaScript">
    ```javascript
    const response = await fetch(
      'https://api.jasni.ai/api/v1/emails?account=you@yourdomain.com&folder=INBOX&limit=10',
      {
        headers: {
          'Authorization': 'Bearer jsk_your_api_key',
        },
      }
    );

    const data = await response.json();
    console.log(data.data.emails);
    ```
  </Tab>
  <Tab title="Python SDK">
    ```python
    result = jasni.emails.list(
        account='you@yourdomain.com',
        folder='INBOX',
        limit=10
    )

    for email in result.emails:
        print(f"From: {email.from_} - Subject: {email.subject}")
    ```
  </Tab>
</Tabs>

## Enable Real-Time Reactions

Set up webhooks so your agent gets notified instantly when emails arrive:

<Tabs>
  <Tab title="TypeScript SDK">
    ```typescript
    const { webhook } = await jasni.webhooks.create({
      url: 'https://your-server.com/webhook',
      events: ['email.received', 'email.sent'],
      description: 'My first webhook'
    })

    // Save the secret! It's only shown once
    console.log('Webhook secret:', webhook.secret)
    ```
  </Tab>
  <Tab title="cURL">
    ```bash
    curl -X POST https://api.jasni.ai/api/v1/webhooks \
      -H "Authorization: Bearer jsk_your_api_key" \
      -H "Content-Type: application/json" \
      -d '{
        "url": "https://your-server.com/webhook",
        "events": ["email.received", "email.sent"],
        "description": "My first webhook"
      }'
    ```
  </Tab>
  <Tab title="JavaScript">
    ```javascript
    const response = await fetch('https://api.jasni.ai/api/v1/webhooks', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer jsk_your_api_key',
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        url: 'https://your-server.com/webhook',
        events: ['email.received', 'email.sent'],
        description: 'My first webhook'
      }),
    });

    const data = await response.json();
    // Save the secret! It's only shown once
    console.log('Webhook secret:', data.data.webhook.secret);
    ```
  </Tab>
  <Tab title="Python SDK">
    ```python
    result = jasni.webhooks.create(
        url='https://your-server.com/webhook',
        events=['email.received', 'email.sent'],
        description='My first webhook'
    )

    # Save the secret! It's only shown once
    print('Webhook secret:', result.webhook.secret)
    ```
  </Tab>
</Tabs>

<Warning>
  **Save your webhook secret!** The `secret` is only returned when the webhook is created. You'll need it to verify incoming webhook payloads.
</Warning>

## Next Steps

<CardGroup cols={2}>
  <Card title="Core Concepts" icon="/icons-svg/book.svg" href="/concepts/inboxes">
    Understand inboxes, messages, and threads
  </Card>
  <Card title="MCP Server" icon="/icons-svg/robot.svg" href="/sdks/mcp/overview">
    Connect AI assistants like Claude directly
  </Card>
  <Card title="API Reference" icon="/icons-svg/docs.svg" href="/api-reference/introduction">
    Explore all endpoints
  </Card>
  <Card title="Webhooks" icon="/icons-svg/webhook.svg" href="/webhooks/overview">
    Get real-time notifications
  </Card>
</CardGroup>
