---
title: "API Reference"
description: "Complete API reference for giving your AI agents email capabilities"
icon: "/icons-svg/code.svg"
---

## Base URL

All API requests should be made to:

```
https://api.jasni.ai/api/v1
```

## Authentication

Include your API key in the `Authorization` header:

```bash
Authorization: Bearer jsk_your_api_key
```

<Card title="Get your API key" icon="/icons-svg/keys.svg" href="/authentication">
  Learn how to create and manage API keys
</Card>

## Request Format

For POST, PUT, and PATCH requests:

```
Content-Type: application/json
```

## Response Format

### Success

```json
{
  "success": true,
  "data": {
    // Response data
  }
}
```

### Error

```json
{
  "success": false,
  "error": "Description of what went wrong"
}
```

## HTTP Status Codes

| Code | Description |
|------|-------------|
| `200` | Success |
| `201` | Created |
| `400` | Bad Request |
| `401` | Unauthorized |
| `403` | Forbidden |
| `404` | Not Found |
| `429` | Rate Limit Exceeded |
| `500` | Server Error |

## Rate Limits

Rate limits are **per account** (not per API key) and vary by plan and operation type:

| Plan | Read | Write | Send Email | Overall |
|------|------|-------|------------|---------|
| Free | 100/min | 30/min | 10/min | 200/min |
| Starter | 500/min | 150/min | 50/min | 1,000/min |
| Pro | 2,000/min | 600/min | 200/min | 5,000/min |

<Note>
  All API keys under your account share the same rate limit pool. Creating multiple keys does not increase your limits.
</Note>

Rate limit headers are included in every response:

| Header | Description |
|--------|-------------|
| `X-RateLimit-Limit` | Maximum requests allowed |
| `X-RateLimit-Remaining` | Requests remaining in current window |
| `X-RateLimit-Reset` | Unix timestamp when the limit resets |

<Tip>
  Implement exponential backoff to handle rate limits gracefully. Check the `Retry-After` header for the recommended wait time.
</Tip>

## Pagination

Use the `limit` parameter to control results:

```bash
GET /api/v1/emails?account=you@domain.com&limit=20
```

## Endpoints

### Accounts

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/accounts` | List email accounts |
| POST | `/accounts` | Create email account |
| PATCH | `/accounts` | Update email account |
| DELETE | `/accounts` | Delete email account |

### Emails

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/emails` | [List emails](/api-reference/emails/list) |
| GET | `/emails/{id}` | [Get email](/api-reference/emails/get) |
| POST | `/emails/send` | [Send email](/api-reference/emails/send) |
| POST | `/emails/{id}/reply` | [Reply to email](/api-reference/emails/reply) |
| POST | `/emails/{id}/forward` | [Forward email](/api-reference/emails/forward) |
| PATCH | `/emails/{id}` | [Update email](/api-reference/emails/update) |
| DELETE | `/emails/{id}` | [Delete email](/api-reference/emails/delete) |

### Drafts

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/emails/drafts` | List drafts |
| POST | `/emails/drafts` | Save draft |
| PUT | `/emails/drafts` | Update draft |
| DELETE | `/emails/drafts` | Delete draft |

### Email Labels

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/emails/{id}/labels` | Get labels for an email |
| POST | `/emails/{id}/labels` | Assign labels to an email |
| DELETE | `/emails/{id}/labels` | Remove labels from an email |

### Labels

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/labels` | [List labels](/api-reference/labels/list) |
| POST | `/labels` | [Create label](/api-reference/labels/create) |
| DELETE | `/labels` | Delete label |

### Webhooks

| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/webhooks` | [List webhooks](/api-reference/webhooks/list) |
| GET | `/webhooks/{id}` | [Get webhook](/api-reference/webhooks/get) |
| POST | `/webhooks` | [Create webhook](/api-reference/webhooks/create) |
| PATCH | `/webhooks` | [Update webhook](/api-reference/webhooks/update) |
| DELETE | `/webhooks` | [Delete webhook](/api-reference/webhooks/delete) |

## OpenAPI Specification

<Card title="Download OpenAPI Spec" icon="/icons-svg/download.svg" href="/api-reference/openapi.json">
  Get the full OpenAPI 3.1 specification for SDK generation or API testing tools
</Card>

## Quick Example

```typescript
// Let your AI agent send an email
const API_KEY = 'jsk_your_api_key';
const BASE_URL = 'https://api.jasni.ai/api/v1';

// Agent sends an email
const response = await fetch(`${BASE_URL}/emails/send`, {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${API_KEY}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    from: 'agent@yourdomain.com',
    to: 'recipient@example.com',
    subject: 'Following up on your request',
    text: 'I have processed your request and here are the results...',
  }),
});

const data = await response.json();
console.log(data);
```

## Need Help?

<Card title="Contact Support" icon="/icons-svg/life-ring.svg" href="mailto:support@jasni.ai">
  Have questions? Our support team is here to help.
</Card>
